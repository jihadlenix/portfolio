# Raw Higgsfield renders live outside this folder (../_source-videos)
# Duplicate of assets/reference/jihad-identity-reference.png
Media/

# OS / editor noise
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp

# Never used here, but keep the repo clean if tooling is ever added
node_modules/
dist/
.env
.env.*
